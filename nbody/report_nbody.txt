1. BENCHMARK ANALYSIS
-------------------------------------------------------------------------------
This report covers the pyperformance n-body benchmark . The
original program is nbody/run_benchmark.py.

The workload is a solar system: the sun plus Jupiter, Saturn, Uranus,
and Neptune. Each timed run does three things: print the total energy,
take 20,000 Euler steps with dt = 0.01, then print the energy again.

The original code uses only the Python standard library plus pyperf. There
is no NumPy. Bodies live in a dictionary. Each body is a position list, a
velocity list, and a mass. There are five bodies, so ten unique pairs.

The hot function is advance(). For every pair it computes the distance
components and r2, then

    mag = dt * r2 ** (-1.5)

which is the same as dt / r^3. That scale is used to update both
velocities (the "kick"). After all pairs, it updates positions.

report_energy() adds kinetic energy and pairwise potential. Before the
run, offset_momentum() zeros the system's total momentum, using the sun
as the reference body.

With only five bodies, this is not a cache or SIMD problem. Almost all
time is Python itself: bytecode, boxed floats, and reading or writing
small lists.


2. UNDERSTANDING PYPERFORMANCE & PROFILING METHODOLOGY
-------------------------------------------------------------------------------
All official numbers come from an Ubuntu QEMU guest running CPython
3.10.12. Stack traces used the course debug build (python3-dbg) for the
original and for the main (CPython) optimized program. Timing always
used regular python3. A debug interpreter is slower on purpose, so it
must not be used for the speedup claim.

We ran pyperf with -w1 -n3 -p1 and 20,000 iterations.
Main compare: nbody_original.json vs nbody_cpython.json
              -> nbody_cpython_compare.txt
Bonus (Numba): nbody_optimized.json -> nbody_compare.txt

Linux perf:
  original:  python3-dbg  -> nbody_baseline.svg, nbody_perf_report.txt
  main opt:  python3-dbg  -> nbody_cpython.svg, nbody_cpython_perf_report.txt
  bonus:     regular python3 (Numba cannot run under python3-dbg)


3. FLAME GRAPH & PROFILING ANALYSIS METHODOLOGY
-------------------------------------------------------------------------------
Call stacks were sampled like this:

    perf record -F 999 -g -- python3-dbg -u nbody/run_benchmark.py

 FlameGraph scripts turned that into SVG:

    perf script | stackcollapse-perf.pl | flamegraph.pl > nbody_baseline.svg


4. DETECTED BOTTLENECKS FROM BASELINE PERF REPORT
-------------------------------------------------------------------------------
The baseline report is nbody/results/nbody_perf_report.txt (about 27,000
samples on python3.10d). The top costs are interpreter overhead, not
gravity math.

  1. The bytecode loop
     _PyEval_EvalFrameDefault took 24.27% of samples by itself. That is
     CPython walking bytecode for advance() and report_energy(). It is
     the largest single item.

  2. Boxed floats and arithmetic wrappers
     Every multiply, add, or ** builds a new Python float. PyFloat_FromDouble
     is 4.62%. Around each operation sit C helpers such as binary_op1
     (2.43%), PyNumber_Multiply (1.06%), and PyNumber_AsSsize_t (2.24%).
     The actual FP helpers (binary_op1, float_mul, float_add)
     add up to only 4.63%. Most samples are scaffolding, not FLOPs.

  3. Short-lived objects and list traffic
     _Py_Dealloc (2.89%) is tearing down those temporary floats.
     list_subscript (1.57%), list_ass_subscript (1.31%), and
     list_ass_item (0.74%) are reading and writing [x, y, z] on every
     pair.

  4. Why this kernel is expensive in Python
     mag = dt * r2 ** (-1.5) is one power and some list math, done
     20,000 * 10 times, all in the interpreter. Five bodies is too
     small for cache or SIMD to matter.


5. IMPLEMENTED OPTIMIZATION IMPROVEMENTS
-------------------------------------------------------------------------------
Same five bodies, dt, step count, and energy. verify_energy.py checks
the original against both optimized programs.

Main optimization (stays in CPython): nbody/optimized_cpython.py

  Replacing ** with sqrt alone, or a SoA of Python lists, was slower
  or about 3%. What works at N=5 is:

    1. Unroll the 10 pairs. No pair iterator, no nested unpack per step.
    2. Keep x/y/z/vx/vy/vz in local variables for the whole advance().
    3. mag = dt / (r2 * sqrt(r2)) instead of r2 ** (-1.5).


Bonus: nbody/optimized/run_benchmark.py

  Structure-of-arrays float64 arrays, mag = dt * inv^3, Numba @njit
  on _advance and _energy, JIT warmed at import. Use this if a much
  larger speedup is allowed. script_nbody.sh can install numpy/numba
  and compare it without overwriting the CPython result files
  (ONLY_CPYTHON=1).


6. PERFORMANCE IMPROVEMENTS & MULTI-METRIC VALIDATION
-------------------------------------------------------------------------------

[A] Main result — CPython only (pyperf)
-------------------------------------------------------------------------------
Guest: Ubuntu, CPython 3.10.12, QEMU CPU.
pyperf -w1 -n3 -p1, 20,000 iterations.
File: nbody/results/nbody_cpython_compare.txt

Metric                    | Baseline            | CPython opt         | Improvement
--------------------------|---------------------|---------------------|-----------
Minimum Time (Best Case)  | 3.573 sec           | 2.482 sec           | 1.44x
Maximum Time (Worst Case) | 3.955 sec           | 2.493 sec           | 1.59x
Standard Deviation (stdev)| 0.22 sec            | 0.01 sec            | -95%
Coefficient of Var (CV)   | 5.9%                | 0.40%               | Stable
Mean Speedup Ratio        | 1.00x               | 1.49x FASTER        | -32.9% time
-------------------------------------------------------------------------------


[B] Main result — same python3-dbg, baseline vs CPython opt
-------------------------------------------------------------------------------
nbody_perf_report.txt vs nbody_cpython_perf_report.txt (both python3.10d).

  _PyEval_EvalFrameDefault: 24.27% self -> 19.77% self
  list_subscript:           1.57% -> 0.01%
  list_ass_subscript:       1.31% -> 0.01%
  list_ass_item:            0.74% -> ~0%

Locals plus an unrolled pair list removed almost all [x,y,z] traffic.
The eval loop is still the top symbol (we are still in bytecode), but
wall time dropped 33%, which matches fewer SUBSCR / iterator ops.
Flame graphs: nbody_baseline.svg and nbody_cpython.svg.

[C] Bonus — Numba (optional)
-------------------------------------------------------------------------------
File: nbody/results/nbody_compare.txt
Mean 3.71 s +- 0.22 s -> 64.1 ms +- 0.3 ms (57.82x).

This compiles the pair loop. The bonus flame graph
(nbody_optimized.svg) is regular python3.

[D] Correctness (energy)
-------------------------------------------------------------------------------
verify_energy.py, 1,000 steps, |d| < 1e-9:

  after offset:      original = CPython = Numba (CPython |d| ~ 3e-17)
  after 1,000 steps: |d| ~ 3e-16 (both optimized programs)



7. HARDWARE ACCELERATION PROPOSAL
-------------------------------------------------------------------------------
The useful floating-point work is the pairwise kick:

    mag = dt / r^3
    v1 -= dr * m2 * mag
    v2 += dr * m1 * mag

The CPython rewrite  removes interpreter work.
Fast square root and fused multiply add are general FP ops.
With only five bodies, a custom block helps if software can call it
cheaply (from C), or if N becomes much larger.

The SystemVerilog unit is nbody/hw/nbody_force.sv. It takes one pair,
forms r2, does square root, runs two Newton
steps, builds inv^3, then writes both velocity updates. Ports are
float32 with valid/ready handshakes.

  inputs:  clk, rst_n, in_valid/ready, dx, dy, dz, m1, m2, dt
  outputs: out_valid/ready, dv1{x,y,z}, dv2{x,y,z}

Two software uses:
  (A) MMIO, one pair at a time.
  (B) New ISA ops (FRSQRT and FMADD) inside a C loop. That helps other
      floating-point kernels too, not only n-body.

                    +------------------+
   advance()        |  CPU / optional FPU
                    +--------+---------+
                             | MMIO / custom ops
                             v
              +--------------+---------------+
              |         nbody_force          |
              |  r2 -> rsqrt/Newton -> inv3  |
              |  mag * m, dr -> dv1, dv2     |
              +------------------------------+

Trade-offs: another Newton iteration costs area and buys accuracy;
SIMD means more ALUs; a higher clock needs more pipeline stages and
power. MMIO for N = 5 is mostly overhead. C plus FRSQRT might be
10-50x versus CPython.


Conclusion
-------------------------------------------------------------------------------
Same 5-body problem and the same energy. The main result stays in
CPython: unroll, locals, and dt/(r2*sqrt(r2)) turn 3.71 s into 2.49 s
on QEMU (1.49x). python3-dbg shows list subscript
traffic essentially gone.

Numba is a bonus run (57.82x) to bypass compilation and interpeter overhead completly. The Verilog
block is the same pairwise 1/r^3 kick in hardware.
===============================================================================
